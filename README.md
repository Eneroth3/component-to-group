# Eneroth Component to Group

Convert components to groups. Useful before making solid operations that cuts
different component instances differently (unless you use those pesky native
solid tools that don't honor components anyway).
